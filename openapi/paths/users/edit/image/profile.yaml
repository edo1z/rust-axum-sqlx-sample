post:
  tags:
    - "user"
  summary: upload profile image
  operationId: edit_user_image_profile
  requestBody:
    description: "Edit user image profile"
    content: 
      application/json:
        schema:
            $ref: "../../../../schemas/user.yaml#/NewUser"
  responses: 
    '200':
      description: return userId
      content:
        application/json:
          schema:
            $ref: "../../../../schemas/user.yaml#/UserId"